# 发给队友：Lumora 1.1.1 在线素材原版运行说明

本版修复中文加解密与双机共享中继配置。两端网页和中继都需更新，旧版通信协议不能与本版混用。请解压到新文件夹测试；若覆盖旧项目，保留自己的 `data`，重新执行 `npm.cmd ci`，停止旧服务后重新启动。不要继续打开旧压缩包解压出的网页。

1.1.1 另修复媒体播放：登录视频默认静音自动播放，点击“开启声音并播放”恢复原视频音轨；背景音乐第一次点击不会再被自动恢复逻辑立即暂停。资源加载失败会明确提示并提供重试按钮。视频与音乐文件没有替换。

## 1. 解压并安装运行环境

如果通过 GitHub 获取：进入[团队仓库](https://github.com/shintamelinda319-sys/lumora-cipher)，点 **Code → Download ZIP** 后解压；会使用 Git 的队友也可以 `git clone https://github.com/shintamelinda319-sys/lumora-cipher.git`，然后 `cd lumora-cipher`。已有克隆先保存/提交自己的修改，再 `git pull --ff-only`，按下面步骤重新安装依赖、重启服务。当前仓库公开，下载无需邀请；若以后改为私有，需先被仓库所有者邀请为协作者。仓库只托管源码，不会自动提供双机公共中继。

将整个压缩包解压到一个普通文件夹（例如 `D:\Lumora-Cipher`），不要在压缩包内直接运行，也不要双击 `index.html`。

安装 Node.js 24 或更新版本，并确认终端能运行 `node -v` 和 `npm.cmd -v`。打包时使用的是 **Node.js 24.19.0 / npm 11.17.0**，`.nvmrc` 记录了该 Node 版本。使用这一版本最便于复现。

## 2. Windows 最简单的启动方式

双击项目根目录的 `start-local.cmd`。首次运行会按 `package-lock.json` 安装依赖，需要联网。安装完成后启动网页，浏览器打开终端显示的 `Local` 地址，通常是 **http://localhost:5173**。

运行期间保留命令窗口；按 `Ctrl+C` 停止。**使用时需要联网**：四段首页风景视频、车窗图片和字体继续访问原来的在线地址，与原作者的实现方式相同。启动脚本不下载这些素材到项目目录。

也可以在解压后的项目文件夹中打开 PowerShell，执行：

```powershell
npm.cmd ci
npm.cmd run dev
```

macOS / Linux 使用相同命令，将 `npm.cmd` 换成 `npm`。不要复制其他电脑的 `node_modules`，也不要用 `npm update` 替代 `npm ci`。

## 3. 与原页面一致的内容

- 四段首页风景视频仍使用原 CDN 地址，通过 `<video>` 播放；播放结束轮播和一秒透明度淡入淡出逻辑不变。
- 车窗 PNG 仍从原 Figma 地址加载；Instrument Serif 正体/斜体仍使用原 Google Fonts 样式表。
- 原本就在本地的登录鲸鱼视频/图片、背景音乐仍随包提供，不改变原始加载方式。
- 页面结构、动画、账户接口和原有英文测试向量保留；古典算法增加中文扩展，双机页新增共享中继地址和服务标识。
- 页面首次进入登录页；可自行注册，也可点击左上角返回首页体验实验。包内没有原作者的账户。

首次进入登录页，鲸鱼视频应立即静音播放而不是停在封面；点击底部“开启声音并播放”后开启视频自带音轨。进入首页和实验页面后播放 Komorebi 背景音乐；若浏览器要求交互，点击“开启背景音乐”提示或右上角扬声器一次即可。音乐跨实验页面继续播放，手动暂停后不会被下一次点击/页面切换擅自恢复。回到登录页时暂停 Komorebi，避免与鲸鱼原视频音轨重叠。

浏览器可能阻止未经交互的有声自动播放，页面不能强行绕过此限制；静音画面不必因此停住。[MDN 自动播放说明](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay)。各电脑的分辨率、缩放、系统中文字体和浏览器版本仍会造成少量显示差异，不能保证跨设备逐像素相同。截图对比请使用相同浏览器、窗口尺寸和 100% 缩放；账户偏好保持“背景自动轮播 / 水面交互”开启，“减少动态效果”关闭。

## 4. 构建后运行

```powershell
npm.cmd run build
npm.cmd start
```

打开 **http://localhost:4173**。源码包不携带 `dist`，所以首次运行 `npm start` 前必须构建。修改源码后，也要重新构建才能影响此端口。

## 5. 自测

```powershell
npm.cmd run test:crypto
npm.cmd run test:unicode
npm.cmd run test:relay
```

手动用例见 `test-data/Lumora-算法测试用例.md` 和 `test-data/中文与双机回归用例.md`，另有中文文本和 JSON 文件可用于上传/传输。

中文界面与跨服务通信回归（先安装 Chrome）：

```powershell
npm.cmd run build
npm.cmd run test:network
npm.cmd run test:media
```

这会启动两个独立服务、使用两个独立浏览器上下文，验证共享中继、7 种算法中文消息、中文/空文件、角色冲突恢复、重连密钥刷新和单机中文往返。自动测试不等同于两台真实电脑的网络连通性验证。若要通过本机的局域网网卡地址测试中继，先设置 `$env:LUMORA_QA_LAN = '1'`；验证开发服务器可设置 `$env:LUMORA_QA_SERVER = 'vite'`。

`test:media` 独立验证首次进入登录页的视频推进、点击开启声音、五个实验页面的首次音乐按钮、键盘开启、页面切换/手动暂停、素材加载失败后重试，以及拒绝有声播放时仍能播放静音画面。测试不使用“无须手势即可播放”的浏览器参数，账号数据也不会写入你的账户库。

完整分享包验证（需要本机安装 Chrome）：

```powershell
npm.cmd run build
npm.cmd run test:share
```

此检查会启动临时本地服务，联网验证四段视频的原始地址、播放、轮播、淡入淡出、外链图片/字体，以及 AES、DH 与双窗口通信。账号测试数据写入独立临时目录，不会改动你的账户库。只有 Edge 时先执行 `$env:LUMORA_BROWSER_CHANNEL = 'msedge'`；自定义浏览器可设置 `LUMORA_BROWSER_PATH`。

## 6. 两台电脑怎么进入同一个房间（重点）

同一局域网不需要先上线网站。两端可以继续各自在本机运行网页，但必须在双机页配置**同一个中继地址**，然后使用相同房间码和相反角色。

1. A、B 两台电脑都用新版源码运行 `start-local.cmd`，各自打开自己的 `http://localhost:5173`（实际端口以终端为准）。
2. 推荐 A 选“加密端”并作为中继主机。在 A 的“双机通信”中展开“查看并分享本机 IP”，找到实际 Wi-Fi/网线网卡对应的 IP 和端口，点击“使用此地址”和“复制给解密端”。例如 IP 为 `192.168.1.10`、端口为 `5173`。A 的终端也会列出候选地址；多网卡时不要选虚拟网卡、VPN 或不互通网段。
3. **A、B 都在“中继主机 IP / 域名”和“端口号”中分别填写 A 的 IP 和端口**。B 不填自己的 IP。不要把 A 的 `localhost` 发给 B；`localhost` 和 `127.0.0.1` 都只代表操作这台电脑自身。单台电脑开两个窗口测试时可以使用 localhost。
4. A 选“加密端”，B 选“解密端”；双方填同一个房间码，例如 `LUMORA-TEST-2026`，分别点击“连接安全房间”。房间最多两人。
5. 确认两端“中继服务标识”一致，DH 完成后会话指纹一致；B 点击“接受该算法”，A 即可发送中文消息。切换算法后需要重新接受。
6. A 服务必须保持运行。换主机/重启服务后标识会变；断线重连后重新交换密钥并协商算法，旧会话密钥不再使用。

若使用构建方式 `npm.cmd run build` / `npm.cmd start`，把上面的 5173 换成 4173（或终端实际端口）。

修改页面中的端口仅改变连接目标，不会启动新的中继服务。加密 / 解密是通信角色，不限制中继必须运行在哪一端；若另选 B 或公共服务器作为中继，两端都填写所选中继主机的地址。公共中继可在“高级设置”中选择 WSS（通常端口 443），并保留实际服务路径（默认 /ws）。

连接失败排查：

- A 的 Node.js 服务需允许通过 Windows **专用网络**防火墙对应端口；不要关闭整个防火墙。浏览器如询问本地网络访问权限，需要允许这个本地实验页面。
- B 在浏览器打开 `http://A的局域网IP:端口/api/relay/info` 应能看到 JSON 服务标识。若打不开，先检查地址、端口、服务是否运行和网络隔离；此地址只用于检查，不要把它当作实验网页入口。
- 同一校园网名称不保证设备互通；访客 Wi-Fi、AP 隔离、不同网段会阻止访问。可换到双方允许访问的同一路由器/热点再测。
- 不在同一网络时，需要双方可访问的公共中继（建议可信 HTTPS/WSS 服务），或已有的可互通 VPN。包内**没有部署公网服务**，代码无法让两个互不连通的 localhost 自动互通。
- 若网页本身是 HTTPS，中继必须使用 `wss://`，不能用 `ws://`。不需要也不应关闭浏览器安全限制。

为什么各自打开 localhost：AES 和 DH 使用 Web Crypto，普通 `http://局域网IP` 页面通常不是安全上下文；本机 localhost 可以使用 Web Crypto。将“本机网页”和“共享中继”分开配置即可保留本地加密能力。[MDN：安全上下文](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Secure_Contexts)、[MDN：WebSocket 客户端](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API/Writing_WebSocket_client_applications)。

安全边界：这是教学演示，房间码不是账户认证，DH 未提供身份认证；须通过可信渠道核对指纹。古典密码、CA 与 MD5 不等于现代安全通信。不要用本演示传真实敏感资料，也不要未经加固就把中继公开到互联网。

### 中文加解密说明

7 种可逆算法均可处理中文、数字、表情与换行。古典四项（单表、多表、多图、置换）遇到中文等内容时，先将 UTF-8 字节可逆地映射为字母，再执行原算法；密文带 `LUMORA-UTF8-V1:算法:` 前缀，解密时须完整保留。不是把中文原样留下，也不是改用 AES 冒充古典算法。

原有标准英文用例的密文保持不变；Playfair 需要补位/J 合并/大小写处理的文本会使用扩展以完整还原。双机古典消息统一使用扩展，保留原消息所有字符。扩展的 MD5 校验只用于发现错误，不是密码学认证。MD5 是单向摘要，本来就没有解密功能。密钥“生成”现在会生成新的随机密钥；解密前不要重新生成密钥。

## 7. 常见问题

- `npm` 不是内部命令：安装 Node.js 后重新打开终端，检查 PATH。
- PowerShell 阻止 `npm.ps1`：使用 `npm.cmd`，无需修改系统执行策略。
- 5173 被占用：使用终端实际显示的地址，或 `npm.cmd run dev -- --port 5174`。
- 安装失败：检查 npm 网络连接，再运行 `npm.cmd ci`；请勿删除锁文件尝试“更新全部依赖”。
- 首页视频/车窗缺失或字体不一致：检查网络能否访问 CloudFront、Figma 和 Google Fonts。原始链接见 `public/assets/ONLINE-SOURCES.json`；外链失效或网络受限会影响展示。
- 登录鲸鱼视频/背景音乐缺失：确认整个 `public/assets` 文件夹已解压，没有只复制 `src`。
- 页面没有声音：更新到 1.1.1 或更新版本，点击播放/音乐按钮；检查浏览器标签页、Windows 音量混合器及输出设备是否静音。代码无法判断扬声器硬件是否真的发声。
- 登录背景静止：确认已更新并刷新；首次静音播放，点击“开启声音并播放”可同时恢复画面和声音。若提示加载失败，检查 `/assets/abyss-whale-login.mp4` 是否能直接播放；背景音乐对应 `/assets/komorebi.mp3`。要完整克隆/解压项目，不能只复制源代码文件夹。
- 账户不存在：分享包刻意不含原有用户数据，首次使用自行注册。

## 8. 包内包含 / 排除

包含：`src`、原本的本地资源 `public`、`scripts`、`test-data`、配置文件、锁文件、后端入口、启动脚本、本文和 `README.md`。`SHARE-MANIFEST.json` 列出源码包内每个文件的大小与 SHA-256，供核对传输完整性。

排除：四段首页视频/车窗/字体的本地下载副本，以及 `.git`、`node_modules`、`dist`、`data`、`.env`、缓存、日志、编辑器私有配置和打包输出。原项目已有账户仍留在原电脑，不会被导出。

重新制作分享包（Windows）：`npm.cmd run pack:share`。生成文件位于 `release` 目录。这个命令按白名单打包，不会递归打包账户目录或旧压缩包。
